firstNumber = +prompt('enter first number')
secondNumber = +prompt('enter second number')

isFirstBigger = firstNumber > secondNumber

if (isFirstBigger) { 
    alert(`${firstNumber} is the bigger one`)
} else {
    alert(`${secondNumber} is the bigger one`)
}

