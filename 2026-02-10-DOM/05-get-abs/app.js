function getAbs(n) {
    return n > 0 ? n : -n
    // if (n > 0) {
    //     return n
    // } else {
    //     return -n
    // }
}

console.log(`abs of 0 is ${getAbs(0)}`)
console.log(`abs of 10 is ${getAbs(10)}`)
console.log(`abs of -10 is ${getAbs(-10)}`)